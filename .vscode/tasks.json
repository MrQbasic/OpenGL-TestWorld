{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g", "-std=c++17", "src/main.cpp", "-o", "bin/main", "-lGL", "-lSDL2", "-lGLEW", "-I", "./lib/"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}